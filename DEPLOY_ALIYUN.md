# 阿里云部署指南（国内域名，低成本方案）

本指南用于将当前个人主页部署到国内可访问域名，采用低成本路径：

- 站点发布：OSS（静态网站托管）
- 全球加速与证书：CDN + HTTPS
- 合规：域名实名认证 + ICP 备案

## 1. 方案说明

当前项目是 Next.js App Router，原始实现包含服务端数据获取和 `revalidate`。

如果要走最低成本的阿里云托管，建议改为静态导出后发布到 OSS。

### 目标架构

1. 域名（阿里云）
2. ICP 备案（阿里云备案系统）
3. OSS Bucket（源站）
4. CDN（加速 + HTTPS）
5. DNS 解析到 CDN CNAME

## 2. 前置准备

1. 阿里云账号完成实名认证。
2. 购买域名（建议 `.com` 或 `.cn`）。
3. 项目已可本地构建。
4. 准备备案信息：
   - 个人身份证
   - 手机号、邮箱
   - 网站名称、服务内容描述

## 3. 第一步：项目静态化（必须）

> 目的：让项目可产出纯静态文件并上传 OSS。

### 3.1 修改 Next 配置

在 `next.config.ts` 启用静态导出（示例）：

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
```

### 3.2 处理运行时数据依赖

你当前页面会请求 GitHub 数据并使用 ISR。静态部署需要改为以下之一：

1. 构建期拉取数据并固化到页面。
2. 降级为静态内容（例如移除实时热力图）。

### 3.3 生成静态产物

```bash
npm install
npm run build
```

构建成功后应生成 `out/` 目录（静态站点文件）。

## 4. 第二步：域名购买与备案

### 4.1 购买域名

路径：阿里云控制台 -> 域名

完成：

1. 域名购买
2. 域名实名认证

### 4.2 提交备案

路径：阿里云控制台 -> ICP 备案

步骤：

1. 新增网站备案
2. 填写主体与网站信息
3. 上传资料并完成人脸核验
4. 等待审核通过

注意：备案未通过前，不建议正式接入国内 CDN 对外服务。

## 5. 第三步：创建 OSS 静态站点

### 5.1 创建 Bucket

路径：对象存储 OSS -> Bucket 列表 -> 创建 Bucket

建议参数：

1. 地域：选择靠近主要访客地区
2. 读写权限：公共读（用于静态站点）
3. 开启静态页面托管
4. 默认首页：`index.html`
5. 默认 404：`404.html`（如有）

### 5.2 上传构建产物

把 `out/` 目录全部上传到 Bucket 根目录。

## 6. 第四步：接入 CDN 与 HTTPS

### 6.1 创建 CDN 加速域名

路径：CDN -> 域名管理 -> 添加域名

配置建议：

1. 业务类型：静态加速
2. 源站类型：OSS 域名
3. 回源 Host：与你源站一致

### 6.2 配置 HTTPS

路径：CDN -> 域名配置 -> HTTPS 配置

1. 申请或上传 SSL 证书
2. 开启 HTTPS
3. 开启 HTTP 强制跳转 HTTPS

### 6.3 域名解析

路径：云解析 DNS

1. 新增 `CNAME` 记录
2. 主机记录：`@` 或 `www`
3. 记录值：CDN 分配的 CNAME

## 7. 第五步：缓存与发布策略

### 7.1 推荐缓存规则

1. `*.html`：短缓存（5-10 分钟）
2. `/_next/static/*`、图片等：长缓存（7-30 天）

### 7.2 发布流程（手动）

1. 本地更新代码
2. `npm run build`
3. 上传 `out/` 到 OSS
4. CDN 刷新目录或关键 URL

## 8. 验证清单

1. 域名可正常访问主页。
2. 浏览器地址栏为 HTTPS 且证书正常。
3. 手机和桌面端页面布局正常。
4. 关键资源无 404（尤其是图片和静态资源）。
5. 网站底部按要求展示备案号（备案完成后）。

## 9. 常见问题

### Q1：为什么不直接把当前版本上传 OSS？

因为当前代码含服务端逻辑与 ISR，OSS 只能托管静态文件，需先静态化。

### Q2：备案要多久？

通常 3-20 天，取决于地区与审核节奏，以阿里云实际进度为准。

### Q3：想保留实时 GitHub 数据怎么办？

改用 ECS/函数计算承载 Node 运行时，再接入阿里云 CDN。
