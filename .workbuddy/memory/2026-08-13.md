# 2026-08-13

- 诊断"直接打开 html 出现灰点"：根因是工作区在 `main` 分支（停在原始提交 5237c20，卡片仍是 `rgba(255,255,255,.045)` 透底）。灰斑修复与方案B左栏只存在于 `hero-variant-b` 分支，未合并进 main。`file://` 直接打开本身没问题，灰点是旧代码所致。已将工作区切到 `hero-variant-b`（确认 line 551 为近实心渐变 `.94` 基底），起本地服务器 http://127.0.0.1:8324 预览。
- 注意：`hero-variant-b` 远程上游已删除（upstream is gone），本地分支仍在；main 上的"无动效极简重构"（PR #2 合并）与动画打磨版是不同设计方向。待用户决定：合并回 main 或重建远程分支。
