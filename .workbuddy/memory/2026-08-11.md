# 2026-08-11

- 应用户要求调整:work/experience/education 条目(含不可点的 article)保留完整 hover 动效,仅 ↗ 箭头保留在真实外链上。用户偏好:视觉动效一致性优先于 affordance 严格性。
- 解决 PR #2 冲突:main(de3ebd3)在 5237c20 后被另一轮无动效极简重构覆盖,与本分支形成设计方向分叉。用户选择以打磨版为准:三个冲突文件全部 checkout --ours,保留 main 的 .gitignore/vercel.json,合并提交 22e43c7 已推送,PR 状态 CLEAN 可合并。注意:本仓库 git fetch/update-ref 写 refs/remotes/origin/* 会静默失败,需手动写 .git/refs/remotes/origin/main 文件绕过。
- 新建分支 design-polish-5237c20(基于 5237c20),先生成 design-preview.html 对比示意页(强调色候选 + ∞ 动效候选)。用户选定 Indigo #818cf8 + Fade+Scale。
- 已实施全部中/高优先级优化 + ∞ 入场动效:引入 --accent indigo(em/stat/tag/hover 四处)、fg-3/fg-4 提亮、统一 em 强调、移除 education 发光 tag、zh-CN 取消负字距、reveal 隐藏态改由 .js 类门控 + GSAP 失败回退可见、移动端汉堡全屏菜单、不可点卡片移除 ↗ 且 hover 动效限定 a.work-item、:focus-visible 焦点框、:active 触屏反馈、锚点 offset -80、语言切换淡入淡出、∞ GSAP fade+scale 入场。JS 语法校验通过,改动尚未提交。
